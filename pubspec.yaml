name: fpvalidate
description: A functional validation library for Dart with support for Either and TaskEither types.
version: 0.5.0
repository: https://github.com/point-source/fpvalidate
issue_tracker: https://github.com/point-source/fpvalidate/issues

environment:
  sdk: ^3.10.0

dependencies:
  fpdart: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
