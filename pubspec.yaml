name: trust_but_verify
description: A fluent validation library for Dart with support for transformation (such as casting) and both sync/async validators.
version: 1.0.0
repository: https://github.com/point-source/trust_but_verify
issue_tracker: https://github.com/point-source/trust_but_verify/issues

environment:
  sdk: ^3.10.0

dependencies:
  fpdart: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
